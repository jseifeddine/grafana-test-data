apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-main
    # VictoriaMetrics internal port is 8428; service name is still "prometheus"
    url: http://prometheus:8428
    access: proxy
    isDefault: true
    jsonData:
      httpMethod: POST
      timeInterval: "15s"
